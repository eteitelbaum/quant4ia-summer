---
title: Working with V-Dem Data
date: today
date-format: long
footer: "[IAFF 6501 Website](https://quant4ia.rocks)"
logo: images/iaff6501-logo.png
from: markdown+emoji
format:
  revealjs:
    theme: [simple, custom.scss]
    transition: fade
    slide-number: true
    chalkboard: true
execute:
  echo: false
  message: false
  warning: false
  freeze: auto
---

## Where Does Data Come From?

<br>

Thoughts? :sunglasses: :thought_balloon:

. . .

:::{.incremental}
- Your boss or a client sends you a file
- Survey data collected by you or someone else 
- You can download it from a website 
- You can scrape it from a website
- A curated collection (e.g. `unvotes`)
- You can access it through an API
:::

## The Concept of "Tidy Data"

<br>

- Each column represents a single variable
- Each row represents a single observation
- Each cell represents a single value

## Tidy Data Example

![](images/tidy_data.jpg){fig-align="center"}

## The Concept of "Clean Data"

<br>

- Column names are easy to work with and are not duplicated
- Missing values have been dealt with
- There are no repeated observations or columns
- There are no blank observations or columns
- The data are in the proper format
  - For example dates should be formatted as dates

## Messy Data Example

![](images/messy_wb_data.jpg){fig-align="center" width=70%}

## Which of These is Likely Tidy/Clean?

<br>

- Your boss or a client sends you a file
- Survey data collected by you or someone else 
- You can download it from a website 
- You can scrape it from a website
- A curated collection (e.g. `unvotes`)
- You can access it through an API

## How Do We Get Tidy/Clean Data? 

<br>

- Wrangle it ourselves
- Use a package where it has been wrangled for us
- Download via an API 

## APIs

<br>

- API stands for "Application Programming Interface"
- Way for two computers to talk to each other

```{mermaid}
%%| fig-align: center
%%| fig-width: 7
%% Note: fig-width option not working as of Quarto 1.4, try again in 1.5 

flowchart LR
    Client-->|Request|id1[(API)]
    id1[(API)]-->|Response|Client
    id1[(API)]-->Server
    Server-->id1[(API)]
```

- For IA, we could use APIs to download social science data

## APIs for IA

::: {.incremental}
- APIs are accessed through packages in R
- Much easier than reading in data from messy flat file!
- Here are a few examples:
  - World Bank data through `wbstats` or `WDI`
  - `countrycode` for country codes
  - `eurostat` for data on Europe
- But there are many APIs out there (please explore!)
:::

## This Lesson

<br>

::: {.incremental}
- Access V-Dem data with `vdemdata` Package
- This is the only package for V-Dem
- Just imports all the data
- So we have to use `dplyr` functions like `filter()` and `select()`
:::

## 

<br>

- Run this code and see what happens. 
- What is `vdem` and what does it do? 

```{r}
#| label: vdemdata1
#| echo: true

# Load packages
library(vdemdata) # to download V-Dem data
library(dplyr)

# Download the data
democracy <- vdem 

# View the data
glimpse(democracy)
```

## `filter()`, `select()`, `mutate()`

<br>

- `filter()` is used to select observations based on their values
- `select()` is used to select variables
- `mutate()` is used to create new variables or modifying existing ones

## `filter()`

- How would you get earlier years?  
- One year? 

```{r}
#| label: vdemdata2
#| echo: true

democracy <- vdem |> 
  filter(year >= 1990) # filter out years > 1989
  
glimpse(democracy)  
```

## `=` versus `==`

<br>

- `=` is used to assign values to variables, just like `<-`
- `==` is used to test if two values are equal to each other
- So `filter(year == 1990)` will give you just the observations for 1990

## `>=` and `<=`

- `>=` is used to test if a value is greater than or equal to another value
- `<=` is used to test if a value is less than or equal to another value
- So `filter(year >= 1990)` will give you the observations for 1990 and later
- And `filter(year <= 1990)` will give you the observations for 1990 and earlier

## `select()`

- Try any indicator listed in the [codebook](https://v-dem.net/documents/24/codebook_v13.pdf) 

```{r}
#| label: vdemdata3
#| echo: true

democracy <- vdem |> # download the V-Dem dataset
  select(                  # select (and rename) these variables
    country = country_name,     # before the = sign is new name  
    vdem_ctry_id = country_id,  # after the = sign is the old name
    year, 
    polyarchy = v2x_polyarchy
  )
  
glimpse(democracy)  
```

## `mutate()`

- Modify the code to create a new variable, e.g. double the value of `polyarchy` 
- How about `polyarchy` squared? 

```{r}
#| label: vdemdata4
#| echo: true

democracy <- vdem |> # download the V-Dem dataset
  filter(year == 2015) |> # keep only observations from 2015
  select(                  # select (and rename) these variables
    country = country_name,     # name before the = sign is new name  
    vdem_ctry_id = country_id,  # name after the = sign is old name
    year, 
    polyarchy = v2x_polyarchy 
    ) |>
  mutate(
    polyarchy_dbl = polyarchy * 2 # create variable 2X polyarchy
  )
  
glimpse(democracy)  
```

## Some Common Arithmetic Operators

<br>

- `+` addition
- `-` subtraction
- `*` multiplication
- `/` division
- `^` exponentiation (also `**`)

## Chaining Steps with `|>`

- We often need to do more than one thing to the same dataset
- The pipe operator `|>` lets us chain those steps together
- Read it from top to bottom: take the result from one step and pass it to the next
- This makes code easier to read because each line does one job

. . .

- Next example: filter rows, select columns, then create or recode variables

## `vdemdata` Example {.smaller}

<br>

```{r}
#| label: vdemdata5
#| output: false
#| echo: true

# Load packages
library(vdemdata) # to download V-Dem data
library(dplyr)

# Download the data
democracy <- vdem |> # download the V-Dem dataset
  filter(year == 2015)  |> # filter year, keep 2015
  select(                  # select (and rename) these variables
    country = country_name,     # the name before the = sign is the new name  
    vdem_ctry_id = country_id,  # the name after the = sign is the old name
    year, 
    polyarchy = v2x_polyarchy, 
    gdp_pc = e_gdppc, 
    region = e_regionpol_6C
    ) |>
  mutate(
    region = case_match(region, # replace the values in region with country names
                     1 ~ "Eastern Europe", 
                     2 ~ "Latin America",  
                     3 ~ "Middle East",   
                     4 ~ "Africa", 
                     5 ~ "The West", 
                     6 ~ "Asia")
  )

# View the data
glimpse(democracy)
```

## 

Use `filter()` to select years...

<br>

```{r}
#| label: vdemdata6
#| eval: false
#| echo: true
#| code-line-numbers: "2-3"

# Download the data
democracy <- vdem |> 
  filter(year == 2015)  |> # keep 2015
  select(                 
    country = country_name,       
    vdem_ctry_id = country_id,  
    year, 
    polyarchy = v2x_polyarchy, 
    gdp_pc = e_gdppc, 
    region = e_regionpol_6C
    ) |>
  mutate(
    region = case_match(region,
                     1 ~ "Eastern Europe", 
                     2 ~ "Latin America",  
                     3 ~ "Middle East",   
                     4 ~ "Africa", 
                     5 ~ "The West", 
                     6 ~ "Asia")
  )
```

## 

Use `select()` to choose variables...

<br>

```{r}
#| label: vdemdata7
#| eval: false
#| echo: true
#| code-line-numbers: "4-11"

# Download the data
democracy <- vdem |> 
  filter(year == 2015)  |> 
  select(                  # select (and rename) these variables
    country = country_name,     # the name before the = sign is the new name  
    vdem_ctry_id = country_id,  # the name after the = sign is the old name
    year, 
    polyarchy = v2x_polyarchy, 
    gdp_pc = e_gdppc, 
    region = e_regionpol_6C
    ) |>
  mutate(
    region = case_match(region, 
                     1 ~ "Eastern Europe", 
                     2 ~ "Latin America",  
                     3 ~ "Middle East",   
                     4 ~ "Africa", 
                     5 ~ "The West", 
                     6 ~ "Asia")
  )
```

## 

Use `mutate` with `case_match()` to Recode Region....

<br>

```{r}
#| label: vdemdata8
#| eval: false
#| echo: true
#| code-line-numbers: "12-23"

# Download the data
democracy <- vdem |>
  filter(year == 2015)  |> 
  select(                  
    country = country_name,     
    vdem_ctry_id = country_id,  
    year, 
    polyarchy = v2x_polyarchy, 
    gdp_pc = e_gdppc, 
    region = e_regionpol_6C
    ) |>
  mutate(
    region = case_match(region, # replace the values in region with country names
                     1 ~ "Eastern Europe", 
                     2 ~ "Latin America",  
                     3 ~ "Middle East",   
                     4 ~ "Africa", 
                     5 ~ "The West", 
                     6 ~ "Asia")
                    # number on the left of the ~ is the V-Dem region code
                    # we are changing the number to the country name on the right
                    # of the equals sign
  )
```

## Save It!

<br>

```{r}
#| label: save-it
#| eval: false
#| echo: true

write_csv(democracy, "democracy.csv")
```

## Your Turn!

<br>

- Keep only observations from `2010`
- Select `country_name`, `year`, and `v2x_polyarchy`
- Rename them to `country`, `year`, and `polyarchy`
- Use `mutate()` to create `polyarchy_triple` and `polyarchy_sq`
- Run `glimpse()` to check your work

```{r}
#| label: timer4

library(countdown)
countdown(minutes = 5, 
          id = "timer4", 
          top = "0%", 
          right = "5%",
          start_immediately = FALSE,
          color_border = "#fff",
          color_text = "#fff",
          color_running_background = "#42affa",
          color_running_text = "black",
          color_finished_background = "#E5D19D",
          color_finished_text = "#00264A")
```

## From Wrangling to Visualization

<br>

- `filter()`, `select()`, and `mutate()` helped us build the dataset we want
- Now we pass that cleaned dataset to `ggplot2`
- Same grammar of graphics idea as before: data + aesthetics + geom + labels
- For a scatter plot, we usually map one variable to the x-axis and one to the y-axis

## Build the Scatter Plot

<br>

- Start with the data and the aesthetics
- Put `gdp_pc` on the x-axis
- Put `polyarchy` on the y-axis
- This sets up the plot before we add any points

```{r}
#| label: scatterplot1
#| eval: false
#| echo: true
#| code-line-numbers: "3"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy))
```

## Build the Scatter Plot

This gives us the axes and plotting space:

```{r}
#| label: scatterplot2

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy))
```

## Add the Points

<br>

- A scatter plot uses `geom_point()`
- We can also map `region` to color to add one more dimension

```{r}
#| label: scatterplot3
#| eval: false
#| echo: true
#| code-line-numbers: "4"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region))
```

## Add the Points

```{r}
#| label: scatterplot4

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region))
```

## Add a Trend Line

<br>

- Then we can improve readability step by step
- Add a trend line with `geom_smooth()`

```{r}
#| label: scatterplot5
#| eval: false
#| echo: true
#| code-line-numbers: "5"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1)
```

## Add a Trend Line

```{r}
#| label: scatterplot6

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1)
```

## Stretch the X-Axis

<br>

- GDP values are very spread out
- A log scale makes the x-axis easier to read

```{r}
#| label: scatterplot7
#| eval: false
#| echo: true
#| code-line-numbers: "6"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k"))
```

## Stretch the X-Axis

```{r}
#| label: scatterplot8

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k"))
```

## Add Labels

<br>

- Then give the plot clearer axis labels, a title, and a legend label

```{r}
#| label: scatterplot9
#| eval: false
#| echo: true
#| code-line-numbers: "7-13"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k")) +
  labs(
    x = "GDP per Capita", 
    y = "Polyarchy Score",
    title = "Wealth and democracy in 2015", 
    caption = "Source: V-Dem Institute", 
    color = "Region"
  )
```

## Add Labels

```{r}
#| label: scatterplot10

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k")) +
  labs(
    x = "GDP per Capita", 
    y = "Polyarchy Score",
    title = "Wealth and democracy in 2015", 
    caption = "Source: V-Dem Institute", 
    color = "Region"
  )
```

## Improve the Color Palette

<br>

- Now swap in a more readable color palette

```{r}
#| label: scatterplot11
#| eval: false
#| echo: true
#| code-line-numbers: "14"

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k")) +
  labs(
    x = "GDP per Capita", 
    y = "Polyarchy Score",
    title = "Wealth and democracy in 2015", 
    caption = "Source: V-Dem Institute", 
    color = "Region"
  ) +
  scale_color_viridis_d(option = "inferno", end = .8)
```

## Improve the Color Palette

```{r}
#| label: scatterplot12

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k")) +
  labs(
    x = "GDP per Capita", 
    y = "Polyarchy Score",
    title = "Wealth and democracy in 2015", 
    caption = "Source: V-Dem Institute", 
    color = "Region"
  ) +
  scale_color_viridis_d(option = "inferno", end = .8)
```

## Finished Scatter Plot

```{r}
#| label: scatterplot13

library(ggplot2)

ggplot(democracy, aes(x = gdp_pc, y = polyarchy)) + 
  geom_point(aes(color = region)) + 
  geom_smooth(method = "lm", linewidth = 1) + 
  scale_x_log10(labels = scales::label_number(prefix = "$", suffix = "k")) +
  labs(
    x = "GDP per Capita", 
    y = "Polyarchy Score",
    title = "Wealth and democracy in 2015", 
    caption = "Source: V-Dem Institute", 
    color = "Region"
  ) +
  scale_color_viridis_d(option = "inferno", end = .8) +
  theme_minimal()
```

## Try it Yourself

- Go to the V-Dem [Codebook](https://www.v-dem.net/documents/24/codebook_v13.pdf) 
- Select a democracy indicator from Part 2.1 (high level indicators) to visualize
- Note the indicator code (e.g. "v2x_polyarchy" for the polyarchy score)
- Change the code and download the data so you can visualize it
- Now make a scatter plot of your indicator versus GDP
